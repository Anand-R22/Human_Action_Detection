# Human_Action_Detection
Human Action Detection is a machine learning project that classifies different human activities using sensor-based data. It includes data analysis, preprocessing, feature scaling, and class balancing, followed by training models like SVM, KNN, Naive Bayes, and Random Forest to predict human actions accurately.
